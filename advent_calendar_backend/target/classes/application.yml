spring:
  datasource:
    url: jdbc:postgresql://${spring.datasource.host:localhost}/TestDB
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
