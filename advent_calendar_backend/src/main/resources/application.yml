spring:
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/TestDB
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
